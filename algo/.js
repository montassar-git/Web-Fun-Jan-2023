const K=16;
const arr[2,5,3,6,7,0,0,23,11];
function consecutiveSums() {
    var arr1 = [];
    for (i=0;i< arr.length;i++){
        if (arr[i] < k) {
            arr1.push(arr[i]);
          }
        }
        return arr1;
      }
